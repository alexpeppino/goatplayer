To install goatplayer, download goatplayer.zip.

The zip file contains:

- goatplayer : the executable file
- 'data', 'help' and 'settings' subdirectories
 
Actually, only 'help' has files. The other 2 contain an empty unuseful file (I created it because I could not create an empty directory).
